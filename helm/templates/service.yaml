apiVersion: v1
kind: Service
metadata:
  name: {{ include "gravity-extractor.fullname" . }}
  labels:
{{ include "gravity-extractor.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "gravity-extractor.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
